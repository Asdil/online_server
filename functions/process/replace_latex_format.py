#!/usr/bin/env python2
# -*- coding: utf-8 -*-
import re

# 手动替换latex公式
def remove_latex_keyword(html):
    html = html.replace(r'\overset', ' overset ')
    html = html.replace(r'\underset', ' underset ')
    html = html.replace('\dfrac', ' dfrac ')
    html = html.replace(r'\times', ' times ')
    html = html.replace(r'\begin', ' begin ')
    html = html.replace('flalign', ' flalign ')
    html = html.replace('align', ' align ')
    html = html.replace('\end', ' end ')
    html = html.replace('\leftrightsquigarrow', ' leftrightsquigarrow ')
    html = html.replace('\longleftrightarrow', ' longleftrightarrow ')
    html = html.replace('\Longleftrightarrow', ' Longleftrightarrow ')
    html = html.replace('\rightleftharpoons', ' rightleftharpoons ')
    html = html.replace('\rightharpoondown', ' rightharpoondown ')
    html = html.replace('\downharpoonright', ' downharpoonright ')
    html = html.replace('\leftharpoondown', ' leftharpoondown ')
    html = html.replace('\downharpoonleft', ' downharpoonleft ')
    html = html.replace('\nleftrightarrow', ' nleftrightarrow ')
    html = html.replace('\nLeftrightarrow', ' nLeftrightarrow ')
    html = html.replace('\leftrightarrow', ' leftrightarrow ')
    html = html.replace('\Leftrightarrow', ' Leftrightarrow ')
    html = html.replace('\longrightarrow', ' longrightarrow ')
    html = html.replace('\Longrightarrow', ' Longrightarrow ')
    html = html.replace('\rightharpoonup', ' rightharpoonup ')
    html = html.replace('\upharpoonright', ' upharpoonright ')
    html = html.replace('\sphericalangle', ' sphericalangle ')
    html = html.replace('\overrightarrow', ' overrightarrow ')
    html = html.replace('\longleftarrow', ' longleftarrow ')
    html = html.replace('\Longleftarrow', ' Longleftarrow ')
    html = html.replace('\leftharpoonup', ' leftharpoonup ')
    html = html.replace('\upharpoonleft', ' upharpoonleft ')
    html = html.replace('\measuredangle', ' measuredangle ')
    html = html.replace('\overleftarrow', ' overleftarrow ')
    html = html.replace('\updownarrow', ' updownarrow ')
    html = html.replace('\Updownarrow', ' Updownarrow ')
    html = html.replace('\nrightarrow', ' nrightarrow ')
    html = html.replace('\nRightarrow', ' nRightarrow ')
    html = html.replace('\diamondsuit', ' diamondsuit ')
    html = html.replace('\varparallel', ' varparallel ')
    html = html.replace('\varepsilon', ' varepsilon ')
    html = html.replace('\varUpsilon', ' varUpsilon ')
    html = html.replace('\rightarrow', ' rightarrow ')
    html = html.replace(r'\Rightarrow', ' Rightarrow ')
    html = html.replace('\nleftarrow', ' nleftarrow ')
    html = html.replace('\nLeftarrow', ' nLeftarrow ')
    html = html.replace('\varnothing', ' varnothing ')
    html = html.replace('\underbrace', ' underbrace ')
    html = html.replace('\varLambda', ' varLambda ')
    html = html.replace('\bigotimes', ' bigotimes ')
    html = html.replace('\vee\wedge', ' veewedge ')
    html = html.replace('\leftarrow', ' leftarrow ')
    html = html.replace('\Leftarrow', ' Leftarrow ')
    html = html.replace('\downarrow', ' downarrow ')
    html = html.replace('\Downarrow', ' Downarrow ')
    html = html.replace('\backslash', ' ')
    html = html.replace('\spadesuit', ' spadesuit ')
    html = html.replace('\heartsuit', ' heartsuit ')
    html = html.replace('\therefore', ' therefore ')
    html = html.replace('\nparallel', ' nparallel ')
    html = html.replace('\underline', ' underline ')
    html = html.replace('\widetilde', ' widetilde ')
    html = html.replace('\overbrace', ' overbrace ')
    html = html.replace('\vartheta', ' vartheta ')
    html = html.replace('\varkappa', ' varkappa ')
    html = html.replace('\varsigma', ' varsigma ')
    html = html.replace('\varGamma', ' varGamma ')
    html = html.replace('\varDelta', ' varDelta ')
    html = html.replace('\varTheta', ' varTheta ')
    html = html.replace('\varSigma', ' varSigma ')
    html = html.replace('\varOmega', ' varOmega ')
    html = html.replace('\leqslant', ' leqslant ')
    html = html.replace('\geqslant', ' geqslant ')
    html = html.replace('\subseteq', ' subseteq ')
    html = html.replace('\supseteq', ' supseteq ')
    html = html.replace('\setminus', ' setminus ')
    html = html.replace('\bigoplus', ' bigoplus ')
    html = html.replace('\bigwedge', ' bigwedge ')
    html = html.replace('\clubsuit', ' clubsuit ')
    html = html.replace('\triangle', ' triangle ')
    html = html.replace('\parallel', ' parallel ')
    html = html.replace('\overline', ' overline ')
    html = html.replace('\epsilon', ' epsilon ')
    html = html.replace('\omicron', ' omicron ')
    html = html.replace('\upsilon', ' upsilon ')
    html = html.replace('\digamma', ' digamma ')
    html = html.replace('\Upsilon', ' Upsilon ')
    html = html.replace('\diamond', ' diamond ')
    html = html.replace('\bigodot', ' bigodot ')
    html = html.replace('\uparrow', ' uparrow ')
    html = html.replace('\Uparrow', ' Uparrow ')
    html = html.replace('\nearrow', ' nearrow ')
    html = html.replace('\searrow', ' searrow ')
    html = html.replace('\swarrow', ' swarrow ')
    html = html.replace('\nwarrow', ' nwarrow ')
    html = html.replace('\bigstar', ' bigstar ')
    html = html.replace('\partial', ' partial ')
    html = html.replace('\because', ' because ')
    html = html.replace('\widehat', ' widehat ')
    html = html.replace('\lambda', ' lambda ')
    html = html.replace('\varrho', ' varrho ')
    html = html.replace('\varphi', ' varphi ')
    html = html.replace('\Lambda', ' Lambda ')
    html = html.replace('\varPhi', ' varPhi ')
    html = html.replace('\varPsi', ' varPsi ')
    html = html.replace('\approx', ' approx ')
    html = html.replace('\subset', ' subset ')
    html = html.replace('\supset', ' supset ')
    html = html.replace('\bullet', ' bullet ')
    html = html.replace('\ominus', ' ominus ')
    html = html.replace('\otimes', ' otimes ')
    html = html.replace('\oslash', ' oslash ')
    html = html.replace('\bigvee', ' bigvee ')
    html = html.replace('\bigcup', ' bigcup ')
    html = html.replace('\bigcap', ' bigcap ')
    html = html.replace('\lbrack', ' lbrack ')
    html = html.replace('\rbrack', ' rbrack ')
    html = html.replace('\lfloor', ' lfloor ')
    html = html.replace('\rfloor', ' rfloor ')
#     html = html.replace('\lbrace', ' lbrace ')
    html = html.replace('\lbrace', ' lbrace ')
    html = html.replace(r'\rbrace', ' rbrace ')
    html = html.replace('\langle', ' langle ')
    html = html.replace('\rangle', ' rangle ')
    html = html.replace('\iiiint', ' iiiint ')
    html = html.replace('\iddots', ' iddots ')
    html = html.replace('\propto', ' propto ')
    html = html.replace('\forall', ' forall ')
    html = html.replace('\exists', ' exists ')
    html = html.replace('\square', ' square ')
    html = html.replace('\arccos', ' arccos ')
    html = html.replace('\limsup', ' limsup ')
    html = html.replace('\alpha', ' alpha ')
    html = html.replace('\gamma', ' gamma ')
    html = html.replace('\delta', ' delta ')
    html = html.replace('\theta', ' theta ')
    html = html.replace('\kappa', ' kappa ')
    html = html.replace('\sigma', ' sigma ')
    html = html.replace('\omega', ' omega ')
    html = html.replace('\varpi', ' varpi ')
    html = html.replace('\Gamma', ' Gamma ')
    html = html.replace('\Delta', ' Delta ')
    html = html.replace('\Theta', ' Theta ')
    html = html.replace('\Sigma', ' Sigma ')
    html = html.replace('\Omega', ' Omega ')
    html = html.replace('\varXi', ' varXi ')
    html = html.replace('\varPi', ' varPi ')
    html = html.replace('\equiv', ' equiv ')
    html = html.replace('\notin', ' notin ')
    html = html.replace('\times', ' times ')
    html = html.replace('\oplus', ' oplus ')
    html = html.replace('\lceil', ' lceil ')
    html = html.replace('\rceil', ' rceil ')
    html = html.replace('\iiint', ' iiint ')
    html = html.replace('\nabla', ' nabla ')
    html = html.replace('\ldots', ' ldots ')
    html = html.replace('\cdots', ' cdots ')
    html = html.replace('\vdots', ' vdots ')
    html = html.replace('\ddots', ' ddots ')
    html = html.replace('\infty', ' infty ')
    html = html.replace('\imath', ' imath ')
    html = html.replace('\jmath', ' jmath ')
    html = html.replace('\aleph', ' aleph ')
    html = html.replace('\angle', ' angle ')
    html = html.replace('\beta', ' beta ')
    html = html.replace('\zeta', ' zeta ')
    html = html.replace('\iota', ' iota ')
    html = html.replace('\nmid', ' nmid ')
    html = html.replace('\cong', ' cong ')
    html = html.replace('\cdot', ' cdot ')
    html = html.replace('\star', ' star ')
    html = html.replace('\circ', ' circ ')
    html = html.replace('\odot', ' odot ')
    html = html.replace('\gets', ' gets ')
    html = html.replace('\vert', ' vert ')
    html = html.replace('\Vert', ' Vert ')
    html = html.replace('\oint', ' oint ')
    html = html.replace('\iint', ' iint ')
    html = html.replace('\hbar', ' hbar ')
    html = html.replace('\lnot', ' lnot ')
    html = html.replace('\land', ' land ')
    html = html.replace('\odot', ' odot ')
    html = html.replace('\perp', ' perp ')
    html = html.replace('\frac', ' frac ')
    html = html.replace('\sinh', ' sinh ')
    html = html.replace('\cosh', ' cosh ')
    html = html.replace('\eta', ' eta ')
    html = html.replace('\rho', ' rho ')
    html = html.replace('\tau', ' tau ')
    html = html.replace('\phi', ' phi ')
    html = html.replace('\chi', ' chi ')
    html = html.replace('\psi', ' psi ')
    html = html.replace('\Phi', ' Phi ')
    html = html.replace('\Psi', ' Psi ')
    html = html.replace('\leq', ' leq ')
    html = html.replace('\geq', ' geq ')
    html = html.replace('\neq', ' neq ')
    html = html.replace('\mid', ' mid ')
    html = html.replace('\sim', ' sim ')
    html = html.replace('\div', ' div ')
    html = html.replace('\ast', ' ast ')
    html = html.replace('\cup', ' cup ')
    html = html.replace('\cap', ' cap ')
    html = html.replace('\int', ' int ')
    html = html.replace('\dif', ' dif ')
    html = html.replace('\ell', ' ell ')
    html = html.replace('\lor', ' lor ')
    html = html.replace('\cos', ' cos ')
    html = html.replace('\csc', ' csc ')
    html = html.replace('\exp', ' exp ')
    html = html.replace('\ker', ' ker ')
    html = html.replace('\min', ' min ')
    html = html.replace('\arc', ' arc ')
    html = html.replace('\deg', ' deg ')
    html = html.replace('\gcd', ' gcd ')
    html = html.replace('\mu', ' mu ')
    html = html.replace('\nu', ' nu ')
    html = html.replace(r'\xi', ' xi ')
    html = html.replace('\pi', ' pi ')
    html = html.replace(r'\Xi', ' Xi ')
    html = html.replace('\Pi', ' Pi ')
    html = html.replace('\lt', ' lt ')
#     html = html.replace('\le', ' le ')
    html = html.replace('\gt', ' gt ')
    html = html.replace('\ge', ' ge ')
    html = html.replace('\ne', ' ne ')
    html = html.replace('\in', ' in ')
    html = html.replace('\ni', ' ni ')
    html = html.replace('\pm', ' pm ')
    html = html.replace('\mp', ' mp ')
    html = html.replace('\to', ' to ')
    html = html.replace('\Re', ' Re ')
    html = html.replace('\Im', ' Im ')
    html = html.replace('\wp', ' wp ')
    html = html.replace('\lg', ' lg ')
    html = html.replace('\ln', ' ln ')
    # html = html.replace('\', '  ')
    html = html.replace('\right', ' right ')
    html = html.replace('\left', ' left ')
    html = html.replace('log', 'log ')


    mol = re.compile('[0-9]mol')
    html = mol.sub(' mol ', html)

    num = re.compile('(\t| )\d(?=[a-zA-Z])')
    html = num.sub(' ', html)

    g = re.compile('[0-9]g')
    html = g.sub(' g ', html)


    mL = re.compile('mL')
    html = mL.sub(' mL ', html)

    html = html.replace('___', ' input ')



    # 手动替换latex字符
    return html